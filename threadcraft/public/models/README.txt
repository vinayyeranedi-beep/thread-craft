Place your real T-shirt model here as:

    tshirt.glb

Then open src/components/TshirtModel.jsx and set:

    const USE_GLB_MODEL = true;

The app currently uses a procedurally generated stand-in shirt (see
src/utils/shirtGeometry.js) so the demo runs with zero external assets.
